var prompt = require('prompt-sync')();
var length = prompt('Please enter the length: ');
var width = prompt('Please enter the width: ');
var area = length * width;
console.log(`Are of the rectangle is: ${area}`)
